config { 
  type: "table",
}
js {
  var { model_dataset } = require("includes/bqml_tutorial/constants");
}

#standardSQL
SELECT
  *
FROM
  ML.EVALUATE(MODEL ${model_dataset}.${ref("penguins_model")},
    (
    SELECT
      *
    FROM
      ${ref("penguins")}
    WHERE
      body_mass_g IS NOT NULL))
