config { 
  type: "table",
}
js {
  var { model_dataset } = require("includes/bqml_tutorial/constants");
}

#standardSQL
SELECT
  *
FROM
  ML.GLOBAL_EXPLAIN(MODEL ${model_dataset}.${ref("penguins_model")})
